build: 'pre


  build


  post

  '
build_deployment: '

  build deployment

  '
clean: '

  clean

  '
dry_run: true
env: {BUILD_PATH: build_path, FLOAT: '1.13', HELLO: world, INTEGER: '1', PATH: '<ROOT>/tests/assets/project/simple/bindir:/opt/rock/runtime/test123/usr/bin:${PATH}',
  ROCK_ENV: local, ROCK_PATH: <ROOT>/tests/assets/project/simple,
  ROCK_RUNTIME: test123, TEST_PATH: test_path}
env_prod: {HELLO: 'better ${HELLO}'}
path: <ROOT>/tests/assets/project/simple
run: echo zero
run_two: echo two
runtime: test123
test: '

  test

  '
verbose: true
