#!/usr/bin/env bash

set -ev

test -f ./requirements.txt

if [[ ! -f ./vendor/virtualenv/bin/activate ]]; then
  virtualenv ./vendor/virtualenv >&2
fi

source ./vendor/virtualenv/bin/activate

pip install -r ./requirements.txt
