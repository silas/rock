env:
  PATH: ./bin:./local/bin:${PATH}

build: |

  test -f ./carton.lock

  carton install --deployment

test: |

  carton exec -Ilib -- prove

tool: |

  carton {args}

run: |

  carton exec -Ilib -- {args}
