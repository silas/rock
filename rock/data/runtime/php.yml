env:
  PATH: ./bin:./vendor/bin:${PATH}

build: |

  test -f ./composer.lock

  composer.phar install

test: |

  php -c ${PHP_INI-.} ./vendor/bin/phpunit --bootstrap ./vendor/autoload.php ${TEST_PATH-./tests}

tool: |

  composer.phar {args}

run: |

  php -c ${PHP_INI-.} {args}
