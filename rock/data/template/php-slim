cat << EOF > .rock.yml
runtime: ${ROCK_RUNTIME-php54}

run: php -S 127.0.0.1:8000 -t public
EOF

rock run composer -n create-project slim/slim-skeleton tmp

shopt -s dotglob

rm -fr tmp/.git*

mv tmp/* .

rmdir tmp
